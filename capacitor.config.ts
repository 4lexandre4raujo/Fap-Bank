import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Digital-account-OCR',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
